dependencies {
    implementation(libs.balmNeoForge) {
        changing = libs.versions.balm.get().contains("SNAPSHOT")
    }
    compileOnly(libs.journeyMapApiCommon)
    compileOnly(libs.blueMap)
    compileOnly(libs.dynmap)
    compileOnly(libs.unbreakablesCommon)
}
